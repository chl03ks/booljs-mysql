{

    "development": {
        "host": "localhost",
        "database": "db"
    },

    "test": {
        "host": "localhost",
        "user": "root",
        "database": "db_test"
    },

    "production": {
        "host": "example.com",
        "database": "db"
    }

}
